apiVersion: v1
kind: Service
metadata:
  name: agentregistry
  namespace: default
  annotations:
    tailscale.com/expose: "true"
spec:
  type: LoadBalancer
  selector:
    app: agentregistry
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
